{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "calendar-range-dialog",
  "type": "registry:block",
  "title": "Calendar Range Dialog",
  "description": "Calendar Range Dialog component variant",
  "files": [
    {
      "path": "src/components/showcase/calendar/calendar-range-dialog.tsx",
      "content": "\"use client\";\n\nimport { Button } from \"@/components/ui/button\";\nimport { Calendar } from \"@/components/ui/calendar\";\nimport {\n  Dialog,\n  DialogContent,\n  DialogDescription,\n  DialogFooter,\n  DialogHeader,\n  DialogTitle,\n  DialogTrigger,\n} from \"@/components/ui/dialog\";\nimport { CalendarIcon } from \"lucide-react\";\nimport { useState } from \"react\";\nimport { DateRange } from \"react-day-picker\";\n\nconst CalendarRangeDialog = () => {\n  const [range, setRange] = useState<DateRange | undefined>({\n    from: new Date(),\n    to: new Date(Date.now() + 7 * 24 * 60 * 60 * 1000),\n  });\n  const [open, setOpen] = useState(false);\n\n  const handleConfirm = () => {\n    setOpen(false);\n  };\n\n  const formatDateRange = () => {\n    if (!range?.from) return \"Select date range\";\n    if (!range?.to) return range.from.toLocaleDateString();\n    return `${range.from.toLocaleDateString()} - ${range.to.toLocaleDateString()}`;\n  };\n\n  return (\n    <div className=\"flex flex-col items-center justify-center w-full gap-3\">\n      <Dialog open={open} onOpenChange={setOpen}>\n        <DialogTrigger asChild>\n          <Button variant=\"outline\" className=\"w-80 justify-start\">\n            <CalendarIcon className=\"h-4 w-4\" />\n            {formatDateRange()}\n          </Button>\n        </DialogTrigger>\n        <DialogContent className=\"sm:max-w-[700px]\">\n          <DialogHeader>\n            <DialogTitle>Select Date Range</DialogTitle>\n            <DialogDescription>\n              Choose a start and end date for your range.\n            </DialogDescription>\n          </DialogHeader>\n          <div className=\"flex justify-center py-4\">\n            <Calendar\n              mode=\"range\"\n              selected={range}\n              onSelect={setRange}\n              numberOfMonths={2}\n            />\n          </div>\n          <DialogFooter>\n            <Button\n              variant=\"outline\"\n              onClick={() => {\n                setRange(undefined);\n              }}\n            >\n              Clear\n            </Button>\n            <Button variant=\"outline\" onClick={() => setOpen(false)}>\n              Cancel\n            </Button>\n            <Button onClick={handleConfirm}>Confirm</Button>\n          </DialogFooter>\n        </DialogContent>\n      </Dialog>\n      {range?.from && range?.to && (\n        <p className=\"text-xs text-slate-400\">\n          Duration:{\" \"}\n          <span className=\"text-slate-200 font-medium\">\n            {Math.ceil(\n              (range.to.getTime() - range.from.getTime()) / (1000 * 60 * 60 * 24)\n            )}{\" \"}\n            days\n          </span>\n        </p>\n      )}\n    </div>\n  );\n};\n\nexport { CalendarRangeDialog }",
      "type": "registry:component",
      "target": "components/calendar-range-dialog.tsx"
    }
  ],
  "categories": [
    "calendar",
    "calendar"
  ],
  "registryDependencies": [
    "button",
    "calendar",
    "dialog"
  ],
  "dependencies": [
    "lucide-react",
    "react-day-picker"
  ]
}